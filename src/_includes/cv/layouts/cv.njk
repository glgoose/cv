---
layout: base
---

<div class="layout h-resume" role="document">
    <header>
        <div class=container>
            {% include "../partials/vcard.njk" %}
        </div>
    </header>

    <main>
        {% include "../partials/sections.njk" %}
    </main>

    <footer>
        <div class="container utl-space-between">
            <div id="footer-meta" class="utl-align-center">
                <p>{{ cv[ lang ].lastUpdated }}: {{ build.timestamp | dateToFormat('dd.MM.yyyy') }}</p>
                <a href="{{ cv.repo }}" class="repo utl-screen-only" target="_blank" rel="noreferer noopener">(view source)</a>
            </div>
            <button class="actions__btn js-print utl-screen-only has-tooltip" aria-label="{{ cv[ lang ].print }}">
                {% icon "print" %}
            </button>
        </div>
    </footer>
</div>